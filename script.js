const toggleMenu = document.getElementsByClassName("toggle-menu")[0];
const linkMenu = dokumnet.getElementsByClassName("link")[0];

toggleMenu.addEventListener("click", () => {
  link.classList.toggle("active");
});
